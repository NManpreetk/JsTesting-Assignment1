function removecharacters(st) {
  return st.replace(st.charAt(st.length - 1), '').replace(st.charAt(0), '')
  //     let myString = someString
  //  let result = myString.slice(1, -1)
  //  return result
}